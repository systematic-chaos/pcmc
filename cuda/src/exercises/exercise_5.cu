/*
 * CUDA program that computes the product of two matrices of `n * n`
 * real elements expressed in simple precision.
 */

#include <stdio.h>
#include <stdlib.h>
#include <cuda_runtime.h>

// CUDA Kernel code for the Device

// Main program in the Host
int main(void) {
    // TODO

    // Reset the Device and exit
    cudaDeviceReset();
    return 0;
}
